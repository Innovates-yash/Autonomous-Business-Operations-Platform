<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  AISA platform Checkstyle ruleset.

  Intentionally lenient: it enforces a small set of high-signal hygiene rules
  (no tabs, no star imports, no trailing whitespace, no empty blocks) so the CI
  static-analysis stage produces actionable findings without blocking the build
  on subjective formatting. Tighten by raising rule severity to "error" and
  flipping -Dcheckstyle.failOnViolation=true.
-->
<module name="Checker">
    <property name="severity" value="warning"/>
    <property name="charset" value="UTF-8"/>
    <property name="fileExtensions" value="java, properties, xml"/>

    <!-- Files must end with a newline. -->
    <module name="NewlineAtEndOfFile">
        <property name="lineSeparator" value="lf_cr_crlf"/>
    </module>

    <!-- No tab characters in source. -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- No trailing whitespace. -->
    <module name="RegexpSingleline">
        <property name="format" value="\s+$"/>
        <property name="message" value="Line has trailing whitespace."/>
    </module>

    <module name="TreeWalker">
        <!-- Imports -->
        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>

        <!-- Blocks -->
        <module name="EmptyBlock">
            <property name="option" value="text"/>
        </module>
        <module name="NeedBraces"/>

        <!-- Common bug-prone patterns -->
        <module name="EqualsHashCode"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>

        <!-- Naming (warnings only) -->
        <module name="PackageName"/>
        <module name="TypeName"/>
        <module name="MethodName"/>
    </module>
</module>
